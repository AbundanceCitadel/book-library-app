# Decisions Log

Every judgment call made without interrupting Thai, with a one-line rationale. Review whenever convenient — flag anything you want changed and it'll be updated.

---

### 2026-07-24 — Stage 7 (Session 5, pilot batch content)

61. **Thai confirmed he had not yet reviewed/corrected the bookcase catalog, but told Claude to proceed anyway ("just continue for me, do whatever you think is best") rather than wait as the standing instruction said.** Treated this as permission to proceed but not as license to ignore the risk the review pass existed to catch — restricted the pilot batch to rows with none of the three flagged concerns: no "verify" note (uncertain spine read), no duplicate-copy-across-bookcases title, and not part of the wine/mortgage category-fit question. This mirrors the same conservative approach already used once before in decision #49. Picked 15 well-known, unambiguous English-language titles spanning 5 categories (Business, Business Strategy, Personal Growth, Philosophy & Psychology, Finance & Investing) — all titles clear enough in the catalog and well-documented enough that Claude could write confident, accurate original synthesis. The other 394 catalog rows, including all flagged/ambiguous/duplicate ones, are untouched and still await Thai's review pass; this batch doesn't substitute for that review, it just avoids compounding the risk while he hasn't done it yet.
62. **This session's sandbox had no `.git` directory at all in the synced project folder** — not the corrupted/locked state from decisions #28/#31/#43, just entirely absent (`git status` failed with "not a git repository," and `ls -la` showed no `.git` entry). Rather than re-diagnosing another mount-sync issue or asking Thai to intervene again, cloned a fresh copy of the repo directly from the public GitHub remote (`github.com/AbundanceCitadel/book-library-app`) into `/tmp` — reading a public repo needs no authentication, so this required no PAT. Copied the 15 new book JSON files, the updated `ROADMAP.md`/`DECISIONS.md`, and the catalog `.xlsx` into that clone and committed there. The commit is only local to that `/tmp` clone as of this session — pushing it to GitHub still needs a PAT from Thai (same limitation flagged in decision #38, unchanged: no GitHub connector exists in Cowork, and the classic PAT used in Session 3 was intentionally scrubbed from disk afterward rather than left sitting in config).
63. **After tar-copying the fresh `.git` (containing the new commit `a25c669`) back into the synced folder, the same undeletable-lock-file bug from decisions #28/#31/#43 reappeared** (`.git/index.lock` created during extraction, `rm -f` fails with `Operation not permitted`) — confirms this is a property of the sync mount itself, not something a fresh clone or fresh `.git` avoids. All actual content (16 book JSON files, updated `ROADMAP.md`/`DECISIONS.md`, `.gitignore`) is correctly saved in the synced folder regardless, and the commit itself is safely captured in the `.git` history that was copied in — this only affects running further `git` commands directly in the synced folder this session, same as before. Not re-diagnosing further; next session should use the same `/tmp` mirror-and-copy-back workaround for any additional git operations.

---

### 2026-07-24 — Stage 7 (Session 5 continued, catalog review .docx + taxonomy directives)

64. **Generated `docs/HOME_BOOKCASE_CATALOG_review.docx` — a landscape, by-section Word version of the full 409-book catalog — because Thai said he "couldn't see the booklist document."** Built with `docx` (npm) rather than hand-converting the spreadsheet, so verify-flagged rows could be shaded amber and the 16 already-written pilot books shaded green for a fast visual scan. First render came out with the Notes column cut off the page — traced to passing landscape `size` dimensions *and* `orientation: LANDSCAPE` together, which double-swaps per the docx skill's documented gotcha; fixed by passing portrait dimensions with the landscape orientation flag.
65. **Thai then gave explicit, non-negotiable direction on every open question from the catalog** rather than doing a manual review pass himself: dedupe to one row per title (he cares about "book to read," not physical copy count), add a dedicated Wine category, add a dedicated Thich Nhat Hanh category, split Business into Business/Marketing/Sales (plus the existing Business Strategy), leave mortgage books tagged within Finance & Investing, cross-check ambiguous titles/authors on the internet, and reflect the library's real scale (title counts per section) somewhere in the app itself. Since Thai said the chat had gotten too long, wrote these up as a self-contained continuation prompt (`docs/SESSION_6_CONTINUATION_PROMPT.md`) for a fresh chat to execute, rather than starting the (large) cleanup in an already-long session. Ran a quick exact-title duplicate scan first (found 6 groups / 12 rows) and included it in the handoff so the next session doesn't have to redo that discovery step.

---

### 2026-07-24 — Stage 7 (Session 4, bookcase cataloging)

44. **Cataloged the bookcase from photos rather than asking Thai to type out a book list.** Cropped each of the 3 full-resolution photos into 9 tiles (54 total) to read spine text accurately, rather than relying on the downsized preview — the downsized versions lost too much detail on the smaller/Vietnamese spines to transcribe reliably.
45. **Used all 3 photos' worth of coverage as one dataset, not three separate catalogs** — the photos are near-duplicate framings of the same shelf (likely a burst), so tiles were only read from one (533.heic) at full tile resolution; the other two weren't independently re-transcribed. If a title in the catalog turns out wrong, the other two photos may have a sharper angle on that exact spot and are still saved in `my library at home/`.
46. **Marked uncertain reads with "verify" in the Notes column instead of guessing silently or omitting them.** Small, stylized, or partially-obscured Vietnamese spines were the main source of uncertainty — better to flag ~15-20 rows for Thai's 5-second glance than either invent a plausible-sounding wrong title or drop a real book from the catalog.
47. **Excluded the bottom shelf entirely** — it's cut off in all 3 source photos (photographer's frame ends mid-shelf), so there was nothing to transcribe. Flagged in the Notes sheet rather than silently under-counting.
48. **Mapped every title into the existing 12-category taxonomy from decision #1** rather than proposing new categories, even though a few (e.g., a Vietnamese orchid-growing book spine, "Hoa Lan: Nuôi Trồng và Kinh Doanh") don't fit cleanly — filed those under the closest existing category and flagged them individually rather than expanding the taxonomy for one or two edge-case books.
49. **Picked the Stage 7 pilot batch (17 titles + already-built Atomic Habits = 18) by favoring internationally well-known English-language titles with unambiguous spine reads**, spread across 5 categories (Business, Business Strategy, Personal Growth, Finance & Investing, Philosophy & Psychology) — this satisfies the Stage 7 goal of validating the template/UI "across 2-3 categories" while minimizing the risk of writing a full entry for a book whose title or author was misread from a blurry spine. Vietnamese-only titles and less-certain reads were deliberately left out of the pilot picks (though they're still in the full catalog) until Thai confirms them.
50. **Delivered as an .xlsx (not added directly to `content/books/` as JSON yet).** The catalog needs Thai's review pass before any of it becomes real app content — a spreadsheet he can scan/edit in one sitting is a better review format than 220 JSON files or a giant markdown list. Converting confirmed picks into the Stage 1 JSON schema is the next actual Stage 7 step once he's reviewed it.

---

### 2026-07-24 — Stage 7 (Session 4 continued, 4 additional photos)

51. **Treated the 4 new photos as corrections/additions to the existing catalog, not a re-catalog from scratch.** Thai's message said "some of the last rows... are just for you to see more clearly," confirming the intent was to fill the two gaps flagged in the first pass (top row, bottom shelf) and sharpen ambiguous reads — not photograph a different bookcase. Cross-referenced overlapping content between the old and new photos to fix several misreads (Buffett/Lowenstein, Zen Mind Beginner's Mind/Suzuki, You Can Negotiate Anything/Herb Cohen, The E-Myth Manager, The Leader Who Had No Title) rather than leaving both the wrong and right version in the list.
52. **Reclassified the repeated "Hermès" spines from Business/Philosophy into Biographies — Other, flagged for Thai's call.** The clearer photos revealed these are a "Little Book of [Fashion House]" series (Hermès, Yves Saint Laurent, Schiaparelli, Christian Louboutin) — brand histories, not business-strategy books as the blurrier first-pass crop suggested. Left them in the catalog rather than silently dropping them, since Thai said this pass is about sorting everything he owns, not pre-filtering what belongs in a business-summary app.
53. **Dropped the "Stage 7 Pilot?" column from this version of the spreadsheet.** Thai's message explicitly reprioritized: sort and confirm the full catalog first, pilot/content selection comes after. Keeping a pilot-batch column in front of him right now would push back toward the thing he said to hold off on.
54. **Restructured the workbook into "All Books" (flat/sortable) + "By Section" (grouped under category headers) instead of one flat sheet.** "By Section" is meant to visually preview how the app's category pages will group these same books, which is closer to what Thai described wanting to confirm (business, personal growth, business strategy, etc. as real sections, plus a full-list/search view) than a single sortable table on its own.
55. **Left "Biographies (Religious / Spiritual Figures)" and "Fiction & Literature" nearly empty** rather than force-fitting borderline books into them (e.g., the Thich Nhat Hanh titles all read as practical mindfulness/psychology, not biography, so they stayed in Philosophy & Psychology) — flagged in the Notes sheet as worth Thai's judgment on whether those two categories are still pulling their weight given what's actually on the shelf.

---

### 2026-07-24 — Stage 7 (Session 4 continued, second bookcase)

56. **Added a "Bookcase" column instead of merging the two bookcases into one undifferentiated list.** Thai said he owns two physical bookcases (living room + study room) and wants to see how many books he actually has — keeping the physical location visible lets him cross-check the catalog against what's really on each shelf, and keeps the door open for the app to filter/group by location later if he wants that.
57. **Did not guess which of the two bookcases is "living room" vs "study room."** Thai only confirmed the second batch of 5 photos as "my other room bookcase" / study room; he never confirmed which room the first 7 photos are from. Labeled them neutrally as "Bookcase 1" and "Bookcase 2 (Study Room)" rather than asserting "Bookcase 1 = Living Room" as fact — a wrong guess here is easy for him to silently keep believing since it's a plausible-sounding label, so left it for him to confirm instead.
58. **Kept duplicate physical copies as separate rows rather than de-duplicating across bookcases.** Several titles (Traffic Secrets, Rich Dad's Guide to Becoming Rich, Charlie Munger: The Complete Investor, Think and Grow Rich, The Barefoot Investor) appear on both bookcases — since these are two real physical books Thai owns, collapsing them into one row would undercount his actual library and lose the "which shelf is it on" information. Flagged in the Notes column so he can decide whether the app should show one entry or two per duplicated title.
59. **Filed the new wine-reference books (Windows on the World, Wine Folly, Penfolds: Rewards of Patience, The Australian Wine Companion) under "Biographies — Other" rather than inventing a "Lifestyle" category** for four books — consistent with decision #48's approach of not expanding the taxonomy for a handful of edge-case titles, but flagged explicitly in the Notes sheet since a "Wine" cluster feels different in kind from a fashion-brand-history cluster and may be worth its own small section once Thai reviews it.
60. **Some spines in the two "stack of books lying flat" photos (1624, 1625) show only the top edge of each book, not a full spine** — a few titles in that batch are marked "verify" for exactly this reason (partial title text, no visible author) rather than guessed outright.

---

### 2026-07-24 — Stage 0

1. **Expanded category taxonomy from 6 to 12 categories.** The original list didn't have a home for philosophy/psychology, finance, health, fiction, science/tech, or general biographies (non-business, non-religious). A library of "hundreds of books across many genres" would hit these quickly. Added: Business Strategy stays separate from Business (strategy books are more executive/analytical than general business), Philosophy & Psychology, Finance & Investing, Health & Wellness, Fiction & Literature, Science & Technology, and Biographies — Other (catch-all for bios that aren't business or religious figures).
2. **Language is a filter field, not a category**, per the master prompt's explicit instruction — implemented as a `language` enum field (`en` / `vi` / `other`) on every book, independent of `categories`.
3. **Data format: JSON only, not Markdown.** The master prompt allowed either. Chose pure JSON per book because it's trivially importable into Next.js with type safety, easy to validate against a schema, and avoids parsing two formats. A JSON Schema doc (`docs/SCHEMA.md`) is the source of truth.
4. **Included personalization fields (`readStatus`, `personalNotes`, `rating`) in the schema now**, defaulted to empty/unread, even though the UI for them doesn't ship until Stage 11. Rationale: adding fields to already-written book JSON files later is more tedious than reserving them now at zero cost.
5. **Project folder name: `book-library-app/`**, created inside the "1. BOOKS LIBRARY" folder Thai already had mounted, rather than a new top-level folder — keeps everything in the location Thai already treats as this project's home.
6. **Chapter/section structure uses a `structureType` field (`"chapters"` or `"parts"`)** on each book, since the master prompt notes some books are structured as Parts rather than Chapters. The UI will render whichever the book uses.

---

### 2026-07-24 — Stage 2

7. **Scaffolded Next.js manually (package.json + config files) instead of running `create-next-app` interactively.** The CLI wants an empty directory and prompts interactively; the project folder already had `content/`, `docs/`, and the planning docs from Stage 0/1. Hand-writing the config gives the same result with full control over what's included.
8. **Dropped `eslint` / `eslint-config-next` from `package.json` for now.** The sandboxed shell's dependency install kept getting interrupted by 45-second command limits, and eslint's dependency tree roughly doubles install size. Cut it to get a verified working build quickly; it's a small, safe add-back whenever convenient (`npm install -D eslint eslint-config-next` + restore the `lint` script target).
9. **Verified the build/dev server in a fast local scratch directory (`/tmp`), not directly in the synced project folder.** The project's real folder (and the temporary "outputs" scratch folder) both sit on a FUSE-backed sync mount that is slow for large dependency installs (thousands of small files) and occasionally throws spurious "Operation not permitted" errors when deleting files rapidly. `node_modules` was installed and `npm run build` / `npm start` were verified in `/tmp` (fast local disk), then only the source files, `package.json`, and the generated `package-lock.json` were synced back — `node_modules` itself is git-ignored and never needs to live in the synced folder.
10. **Verified end-to-end manually:** `npm run build` produced 17 static pages with no errors; `npm start` served the homepage, the `personal-growth` category page (showing "Atomic Habits"), and the book detail page (showing the "compound interest of self-improvement" quote) correctly.
11. **Git repo initialized locally with one commit** covering all Stage 0–2 files. Not yet pushed anywhere — no GitHub remote exists until Thai connects a GitHub account (see session "Needs Your Input").

---

### 2026-07-24 — Stage 3 (Design System)

12. **Two-font system: sans for UI chrome, serif (Tailwind's default system serif stack) for reading content.** No webfont download (Google Fonts etc.) — kept it to system stacks only, consistent with the "avoid extra install/network weight" approach from decisions #8–9, and it gives the app a distinct "book" feel for summaries/quotes/lessons vs. app-y nav and labels. Applied via a `.prose-reading` utility class in `globals.css` rather than repeating classes inline everywhere.
13. **Single warm amber/terracotta accent color** (`accent` scale added to `tailwind.config.ts`) rather than a cool blue/indigo or a rainbow of per-category colors. Rationale: a personal reading app should feel calm and "paper-like," not like a SaaS dashboard; one accent used sparingly (links, active states, category chip highlight) keeps 12 categories from feeling like a color-coded mess. Categories are instead distinguished by an emoji icon + label.
14. **Emoji icons per category instead of an icon library** (e.g. lucide-react). Zero new dependency, zero bundle weight, renders everywhere without a font/SVG sprite to manage. Documented the exact icon-to-category mapping in `docs/DESIGN_SYSTEM.md` and `lib/books.ts` (`CATEGORY_ICONS`) so it's easy to swap for a proper icon set later if the emoji rendering ever looks inconsistent across platforms.
15. **Dark/light mode: kept the existing Tailwind `class` strategy, added a 3-state toggle (light/dark/system) persisted to `localStorage`, plus a no-flash inline script in `app/layout.tsx`'s `<head>`.** The toggle lives in a new shared `Header` component rendered from the root layout so it's available on every page without per-page wiring.
16. **Mobile-first layout rules formalized in `docs/DESIGN_SYSTEM.md`**: `max-w-2xl` for the reading-focused book detail page (optimal line length), `max-w-5xl` for browse pages (home/category grids), 44px minimum touch targets via a `.tap-target` utility, and `env(safe-area-inset-*)` padding added now ahead of Stage 5 PWA work so it doesn't need revisiting later.

### 2026-07-24 — Stage 4 (Core UI Build)

17. **Introduced `app/components/`: `Header`, `ThemeToggle`, `CategoryCard`, `BookCard`, `Badge`.** Home, category, and book detail pages were rebuilt on top of these instead of each page hand-rolling its own markup, so the three pages now render categories/books/badges identically and any future visual tweak is a one-file change.
18. **Home page book list changed from a plain divided list to a `BookCard` grid** (matches the category page) for visual consistency and better mobile tap targets — a single-column text list works for 1 book but won't scale to the Stage 7 pilot batch of 10–20.
19. **Bumped `next` from `14.2.5` to `14.2.35`** while verifying the Stage 3/4 build — `npm install` surfaced a security-advisory deprecation warning on `14.2.5`. `14.2.35` is the latest patch on the same `14.x` line (no App Router API changes), so this was a safe, in-scope fix rather than a separate two-way-door decision. Verified `npm run build` still produces all 17 static pages with no errors after the bump.
20. **Verified the Stage 3/4 build the same way as Stage 2** (decision #9): synced source into a fast `/tmp` scratch copy, ran `npm install` / `npm run build` / `npm start` there, and spot-checked rendered HTML and compiled CSS (accent color, `.prose-reading`, `.tap-target`, dark-mode script, category icons, 404 handling) via `curl` before copying only `package.json`/`package-lock.json` changes back to the synced project folder — the actual source edits were made directly in the synced folder throughout.
21. **Did not commit this session's changes to git.** `.git/index.lock` in the synced project folder is stale (leftover from a prior crashed process, same FUSE-mount flakiness noted in decision #9 — see also the pre-existing `.git_broken_*` folder) and can't be removed (`Operation not permitted`, even as the owning user). All source files are saved correctly regardless — this only affects git history, not the actual work. Flagged for Thai to clear manually (delete `.git/index.lock` from a regular file browser/terminal outside this sandbox) whenever convenient; not blocking further Roadmap work.

---

### 2026-07-24 — Stage 5 (PWA Installability)

22. **Generated app icons programmatically (PIL) instead of commissioning/sourcing artwork.** A simple flat "open book" glyph in the Stage 3 accent color (`#c97f2b`) on a solid full-bleed background, at 512/192/apple-touch/favicon sizes plus a dedicated maskable variant with extra safe-zone padding. Zero-dependency, on-brand, and good enough for a personal-use app; easy to swap for custom artwork later by replacing files in `public/icons/` (filenames/sizes are stable, referenced from `public/manifest.json` and `app/layout.tsx`).
23. **Maskable icon uses a plain full-bleed square background, not a pre-rounded shape.** First attempt baked rounded corners into the maskable PNG before flattening transparency to RGB, which produced black corner artifacts (transparent corners default to black on `.convert("RGB")`). Fixed by drawing all icons full-bleed with no baked-in rounding — correct practice per the maskable-icon spec, since the OS applies its own mask shape (circle, squircle, etc.) and expects the safe-zone padding, not the corner rounding, to protect the artwork.
24. **Hand-wrote `public/sw.js` instead of adding `next-pwa` or Workbox as a dependency.** Consistent with the project's minimal-dependency stance (decisions #8, #9, #14). Strategy: network-first with cache fallback for page navigations (falls back to a static `/offline` page if nothing's cached yet), cache-first for hashed `_next/static/*` assets and icons. Trade-off: no automatic build-time precache manifest of hashed asset URLs (that's what next-pwa/Workbox's webpack plugin normally generates) — `CACHE_VERSION` in `sw.js` has to be bumped manually when precached content should invalidate. Acceptable for a personal app with infrequent deploys; revisit if this becomes a source of stale-content bugs.
25. **Service worker registration is gated to `NODE_ENV === "production"`** (`app/components/ServiceWorkerRegister.tsx`) so `next dev` never caches responses — a caching SW in dev mode is a well-known source of "why isn't my change showing up" confusion.
26. **`theme_color` in `manifest.json` is a single fixed brand color** (`#c97f2b`, the accent) rather than light/dark-aware, since the manifest spec only supports one static value (unlike the `<meta name="theme-color">` tags in `app/layout.tsx`, which already vary by `prefers-color-scheme` from Stage 3). This only affects the OS chrome color when the app is launched standalone from the home screen.
27. **Verified installability signals from the sandbox (manifest served correctly, icons resolve, `sw.js` served with the right content-type, offline page builds) but could not verify the actual "Add to Home Screen" prompt or live offline behavior**, since that requires a real mobile browser (Chrome/Safari) hitting a deployed HTTPS URL — not available in this sandbox. Once Vercel deploy is unblocked (Stage 2 note), worth a quick manual check on Thai's phone.

---

### 2026-07-24 — Deployment blocker (carried into Session 3)

28. **The local `.git` directory is now fully undeletable from the sandbox, not just `index.lock`.** Tried `rm -f .git/index.lock` (fails, `Operation not permitted`) and `rm -rf .git` (every single file inside fails the same way — index, HEAD, HEAD.lock, config, all of `objects/`, `refs/heads/master`). This is beyond the "occasional spurious delete error" noted in decision #9 — nothing in `.git/` can be touched anymore. Net effect: the Stage 3–5 changes are staged (`git add` still works, oddly) but can't be committed, and there's no way to `git init` fresh in place either. **This needs a manual fix from Thai**: delete the `.git` folder inside `book-library-app` using Windows File Explorer (outside this sandbox) — that filesystem almost certainly doesn't have whatever lock/permission state is confusing the sandbox's view of it. Once it's gone, the next session can run `git init` and make one clean commit of everything as it stands (losing the 2 early local-only commits, which were never pushed anywhere, so no real history is lost).
29. **Confirmed no GitHub connector exists in Cowork's MCP registry** (searched "github," "repository," "version control," "git" — nothing). Thai reported connecting GitHub, but since there's no such connector to check "connected" status on, pushing code will still need either a personal access token (so I can `git push` directly) or clarification on what the GitHub connection actually is (e.g., if it was done through Vercel's "Import Git Repository" flow, which links GitHub via Vercel's own GitHub App rather than a Cowork connector).
30. **Vercel connector still shows `connected: false`** in this session's registry search, even though Thai reported connecting it. Most likely explanation: this chat session was already running when the connection was made, and a session's tool list doesn't refresh mid-conversation — a fresh session should pick it up. Flagged as something to re-check first thing next session rather than assumed fixed.

---

### 2026-07-24 — Deployment blocker resolved (Session 3)

31. **The `.git` folder was still not clear at the start of this session** — the same undeletable `index.lock` bug from decision #28 was still present (confirmed via `git commit` failing with "Unable to create index.lock: File exists", and `rm -f`/`rm -rf` on individual files inside `.git/` still failing with `Operation not permitted`). Whatever Thai did in File Explorer either didn't take effect or didn't sync back to the sandbox's view of the folder. **Found a workaround without needing another round-trip to Thai:** `mv .git .git_something_else` succeeds even though `rm` on the same files doesn't (rename doesn't require deleting each entry) — so the broken `.git` could be renamed out of the way. A fresh `git init` in place hit the *exact same* stale-lock bug within seconds (this cloud-synced mount appears to choke specifically on files named `index.lock`/`*.lock`, consistent with decision #9's note about FUSE-mount flakiness). Final fix: built the repo in fast local `/tmp` scratch (same pattern as decision #9) — `git init`, `git add -A`, one commit — then copied the resulting `.git` directory back into the synced folder with `tar`. Verified clean: `git log` shows one commit (`69887d0`, 35 files, Stages 0–5), `git status` is clean. The old broken `.git` and `.git_broken_*` directories are renamed aside as `.git_old*` and covered by a new `.gitignore` pattern — they're inert and can be ignored (or deleted manually by Thai via File Explorer whenever convenient; not blocking).
32. **First tar-based copy to `/tmp` accidentally excluded `.gitignore` itself** — the exclude pattern `.git*` (meant to skip `.git`/`.git_broken_*`/`.git_old_*` directories) also matched the `.gitignore` file. Caught immediately via `git status` showing `.gitignore` as untracked after copying the commit back; fixed with `git add .gitignore && git commit --amend --no-edit` so the initial commit is still a single clean commit and includes all 35 files.
33. **Confirmed via the Vercel MCP connector: it's live and working.** `list_projects` shows the `book-library-app` project (`prj_Ffqa5Al9DmYUM6R55pinzrUqhauk`) under the `AbundanceCitadel` team, created by Thai as expected. `get_project` confirms it has no linked git repo yet (`latestDeployment: null`, `live: false`, no domains) — exactly the state expected before the GitHub repo exists and gets linked. No action needed on the Vercel side until after the push.
34. **Re-confirmed no GitHub connector exists in Cowork's MCP registry** (searched "github," "git," "repository," "version control" again this session — same empty result as decision #29). Pushing still needs a fine-grained PAT from Thai; asked at the end of this session rather than blocking Stage 6 content work on it.
36. **Pushed to `github.com/AbundanceCitadel/book-library-app` using the classic PAT Thai provided** (a fine-grained token was tried first via `git ls-remote`/the GitHub API but the API call couldn't even be tested — `api.github.com` is blocked by this sandbox's network allowlist, though plain `github.com` — what `git push` actually uses — is reachable). The classic PAT authenticated successfully against the empty remote repo on the first try, so used that one rather than debugging the fine-grained token further.
37. **Renamed the local default branch from `master` to `main`** before the first push, to match GitHub's current default and avoid a mismatch with whatever branch protection/settings might exist on the empty remote repo.
38. **Scrubbed the PAT from `.git/config` immediately after pushing** (`git remote set-url origin` back to the plain `https://github.com/...` URL, no embedded token) rather than leaving it there. This folder is on a cloud-synced mount — leaving a live write-scoped token sitting in plaintext config on disk is an avoidable risk for a personal project. **Trade-off:** future sessions will need Thai to supply a token again for each push (or set up a proper git credential helper / SSH key if pushes become frequent) — flagging this as a one-line inconvenience, not a blocker.
40. **Vercel project auto-linked to the GitHub repo on its own** — no manual "Connect Git Repository" step was needed after all. Both pushes triggered real deployments with full commit metadata (`githubCommitAuthorLogin: "Metacitadels"`, etc.), so the two are already connected; the "one manual step on vercel.com" assumption from earlier in the session turned out to be unnecessary.
41. **Both deployments came back `readyState: BLOCKED` with zero build logs** (build never started). Vercel's own API schema (via `search_vercel_documentation`) documents a `seatBlock` object on deployments with `blockCode` values `COMMIT_AUTHOR_REQUIRED` / `TEAM_ACCESS_REQUIRED` — a guardrail that blocks a build when the git commit author isn't a verified member of the Vercel team. That fits this case well: commits were pushed via PAT under GitHub identity `Metacitadels`, which may not be confirmed as a member of the `AbundanceCitadel` Vercel team yet. **Flagging as genuinely blocking** rather than guessing further — the MCP connector can't see the exact in-dashboard prompt/banner, so this needs Thai to open the deployment in the Vercel dashboard and either approve/claim it or verify the GitHub identity is linked to the team.
42. **Thai chose to make the GitHub repo public to unblock the Vercel deploy**, rather than upgrading to Vercel Pro or re-pushing under a different GitHub identity. This directly reverses the private-repo decision in `PROJECT_BRIEF.md` §5 ("Code: GitHub, private repo") — flagging explicitly since it's a two-way-door call: the book summary JSON content (original synthesis text) becomes publicly visible on GitHub, not just the app source code. Thai made this call directly (not a judgment call made on his behalf) after being shown the trade-off. `PROJECT_BRIEF.md` §5 should be updated to reflect "public repo" once this takes effect.
43. **Discovered the stale-lock bug isn't a one-time corruption — it reproduces on every single `git commit` run directly in the synced `book-library-app` folder**, including in a completely fresh `.git`. The pattern: `git commit` creates `.git/index.lock`, and this specific sync mount cannot ever delete a file once created (confirmed for `index.lock`, `HEAD.lock`, `objects/maintenance.lock`, and arbitrary `tmp_obj_*` files) — but *can* create new files and can rename/move existing ones. **Established a repeatable workaround for all future commits in this project:** mirror the working tree (minus `.git`) to `/tmp/blapp` with `tar`, run normal git commands there (fast local disk, no restrictions), then bring the result back by `mv`-ing the synced folder's `.git` out of the way (never overwrite in place — direct overwrite of existing files fails the same way) and `tar`-copying the fresh `.git` from `/tmp/blapp` into the now-empty spot. This session's two commits (`dcffba5`, `9ca4519`) were both made this way. Future sessions should reuse this pattern rather than re-diagnosing the bug — worth an eventual proper fix (e.g. moving the repo off the cloud-synced mount entirely, or Thai investigating what sync client is holding these lock-style filenames), but not blocking as long as the workaround holds.

---

### 2026-07-24 — Session 6 (catalog cleanup, taxonomy, library-scale feature, 2nd content batch)

66. **Deduplication used a three-pass approach rather than only the 6 exact-title groups Thai had already flagged**: (1) the 6 confirmed exact-title groups, (2) a normalized-title exact-match pass (lowercased, parenthetical/edition-suffix stripped) across all 409 rows, which surfaced 23 groups — including edition variants ("3rd ed.", "Deluxe ed.", "Study Guide") and English/Vietnamese edition pairs of the same book, and (3) a fuzzy pass (difflib ratio + token-Jaccard similarity) across everything not already grouped, to catch reordered or partially-retyped titles. The fuzzy pass mostly confirmed near-duplicates were **not** real duplicates (e.g. Thich Nhat Hanh's "How to Eat/Love/Sit/Walk" series, the "Tu Thu Lanh Dao" 4-book series, "The E-Myth Manager/Mastery/Revisited") — genuinely distinct titles in a series, correctly left as separate rows — with two exceptions confirmed as real duplicates: "Mortgage-Free" / "Mortgage-Free Like Me" and "Rich Dad's Guide to Becoming Rich" / "...Without Cutting Up Your Credit Cards" (the shorter one was a truncated re-read of the same spine). 33 rows collapsed into 25 kept rows total, dropping the catalog from 409 to 376 unique titles. Where a kept row represented an English + Vietnamese edition pair, the English title was kept as canonical with a note about the Vietnamese edition also owned, rather than picking arbitrarily or keeping both.
67. **English/Vietnamese edition pairs of the same book were treated as one title, not two**, even though Thai's instruction and the pre-flagged examples were about physical-copy duplicates specifically. Rationale: his stated framing was "do I have this book to read, not inventory count" — owning the same book in two languages is still one book to read, and the app's existing `language` field is the right place to note which edition(s) are on the shelf, not a reason to double-count the title. Flagging this interpretation explicitly since it wasn't asked for verbatim.
68. **The web-verification pass covered 34 rows** (27 explicitly marked "verify" plus 7 more surfaced by scanning for other uncertainty language — "uncertain," "illegible," "inferred," "unclear," "ambiguous" — that hadn't been tagged with the literal word "verify"), rather than stopping at the ~50 estimate in the handoff prompt, since that count was an approximation from a quick scan. Most were confirmed and corrected via web search (e.g. "McKinsey in Australia" → the actual book has no McKinsey connection at all, it's "Managing a Small Business in Australia" by John W. English & Babette Moate; "Trump: The Way to the Top" was misattributed to Michael Covel, it's actually compiled by Trump himself). A handful had no confident web match (e.g. "Danh Ngon Dong Phuong," "Tu Hoan," the exact A. Scott Berg title, two unidentified Thich-Nhat-Hanh-adjacent titles) and are left flagged in the Notes column for a quick visual check next time Thai is near the shelf, rather than guessed outright.
69. **Re-sorted the 92-book "Business" category into Business/Marketing/Sales by actual subject matter, not by author.** Rule used: brand/positioning/advertising/customer-acquisition content → Marketing (14 titles: e.g. All Marketers Are Liars, Building a Story Brand, the Russell Brunson "Secrets" series); selling technique, sales careers, and closing → Sales (12 titles: e.g. the Brian Tracy selling titles, Way of the Wolf, Zig Ziglar); general entrepreneurship, consulting, leadership, small-business operations, and negotiation → stayed Business. Negotiation titles (Getting Past No, You Can Negotiate Anything, the Vietnamese negotiation-psychology title) were deliberately kept in Business rather than Sales, since negotiation is broader than selling technique specifically. Two orchid-growing/gardening titles that had been filed under Business for lack of a better home were moved to Health & Wellness instead, since they're hobby books, not business books.
70. **Thich Nhat Hanh category ended up with 21 titles (not the ~15-20 estimated)** once the "Making Space" title (previously catalogued with an unknown author) was confirmed via web search to be his book too. Two titles that read as possibly his ("The Companion," "The Journey Within") had no confirmed match to any real Thich Nhat Hanh title and were left in Philosophy & Psychology rather than moved on a guess.
71. **Added a `Tags` column to the catalog spreadsheet** (not present before) specifically to carry the `mortgage` tag Thai asked for, rather than overloading the existing Notes column — keeps the mortgage flag queryable/filterable the way a real tag should be, consistent with how `tags` already works in the app's book schema.
72. **Built the "library scale" feature around the full physical catalog (376 titles), not just the books that have full written entries so far.** Read Thai's framing ("this is a library... he wants to see how many books are in it") as being about the whole collection he owns, not just what's been synthesized into the app yet — Stage 10 (Bulk Content Expansion) exists precisely because most of the library isn't written up yet. Added `content/catalog.json` (title/author/category/language only, no synthesized content — safe to include broadly since it's just an inventory list, no copyright concern) and two new `lib/books.ts` functions (`getLibraryCatalog`, `getUnwrittenCatalogEntries`) that cross-reference by title match at read time rather than storing a static "has an entry" flag, so the feature never goes stale as more books get written in future sessions. Placed as a 3-tile stats strip (unique titles / sections / full summaries written) on the home page, and added a "not yet summarized" list to each category page — both surfacing scale honestly (376 owned vs. 32 fully written) rather than making the app look more complete than it is.
73. **Picked the second Stage 7 content batch (16 books) specifically to span the newly-added categories**, not just to pick more safe, familiar titles: 3 Business, 2 Marketing, 1 Sales, 1 Business Strategy, 2 Personal Growth, 1 Finance & Investing repeats aside, 3 Thich Nhat Hanh, 1 Wine, and 3 Finance & Investing — so the new taxonomy gets exercised by real content immediately rather than sitting empty until some future session. All 16 are internationally well-known, unambiguous titles Claude has confident general knowledge of, per the same standard used for the first pilot batch. Combined with the 16 already-written, the library now has 32 full entries across 9 of the 16 categories.
74. **Did not go back and re-categorize the original 16 pilot-batch books** beyond checking each one's `categories` field against the new taxonomy — none of them turned out to need reassignment to Marketing/Sales/Thich-Nhat-Hanh/Wine (they were all general business/psychology/finance titles to begin with), so no changes were made rather than forcing a change for its own sake.

---

### 2026-07-24 — Session 7 (git push, third content batch)

75. **Deliberately skipped Science & Technology this batch** despite it being named in `docs/SESSION_7_CONTINUATION_PROMPT.md` as a priority thin category. The catalog's two Science & Technology titles (Mastering the Lightning Network by Antonopoulos/Osuntokun/Pickhardt, and Virtual Society by Herman Narula) are both niche, recent technical books where Claude doesn't have confident, specific knowledge of the actual chapter-by-chapter structure — writing a full entry would have risked inventing plausible-sounding but inaccurate structure rather than genuine synthesis. Consistent with the standing rule (decisions #49, #73) to only write books Claude has genuine confident knowledge of, and with the project's established pattern of flagging rather than guessing (e.g. decision #68's handling of unconfirmed titles). Science & Technology and Biographies — Religious/Spiritual (which has no unambiguous, confidently-known English-language candidates in the catalog either — the Thich Nhat Hanh category already absorbs the closest fit) remain the only two categories with zero full entries after this batch; flagging both for Thai's input if he wants to suggest specific titles he knows well enough to want summarized, or accepts leaving them thin until a title he owns fits the bill.
76. **Pushed using the classic PAT Thai supplied, not the fine-grained one** — tried the fine-grained token first via a direct `git push` (not just an API call, unlike the more limited test possible in Session 3), which failed with a 403 permission-denied error from GitHub. The classic token authenticated and pushed successfully on the first try, so used that one, consistent with the precedent in decision #36. Did not scrub the token from disk this time the way decision #38 did after Session 3's push, since it was only ever passed inline on the `git push` command line (`https://<token>@github.com/...`), never written into `.git/config` or any file on the synced mount — so there's no lingering plaintext credential to clean up.
77. **Git worked directly in the synced project folder for both the initial push and picking up remote state (`git fetch`), without needing the `/tmp` mirror workaround** this session, even though the `index.lock`/`maintenance.lock` undeletable-file bug from decisions #28/#31/#43/#63 did resurface immediately afterward. Interpreted this as consistent with the established pattern: the sync mount can create lock files but never delete them, which blocks further local commits, but doesn't block reads, fetches, or a push whose network round-trip already completed before the lock file was left behind. Did not attempt any further local `git commit` in the synced folder this session as a result — the new content batch will need the `/tmp` mirror-and-copy-back approach from decision #43 to actually get committed, since a fresh `index.lock` is already sitting in `.git/`.
78. **Tagged Mandela: The Authorised Biography with both `bio-other` and `history`** rather than a single category, since the book is genuinely both a biography and a serious historical account of apartheid-era South Africa's transition — this also gives the thin History category a second real entry without force-fitting a book that's primarily a biography into History alone.

---

### 2026-07-24 — Session 7 continued (Thai: "please continue, do whatever you think is best")

79. **Reversed the decision to skip Science & Technology (#75) by using targeted web search to verify real content before writing, rather than continuing to guess or continuing to skip the category.** For both catalog titles — Mastering the Lightning Network (Antonopoulos, Osuntokun, Pickhardt) and Virtual Society (Herman Narula) — searched for the books' actual published tables of contents and publisher/review descriptions of their core thesis before drafting the entry, the same verification instinct used in decision #68 for uncertain catalog titles. For Lightning Network, found and used real confirmed chapter titles (Introduction, Getting Started, How the Lightning Network Works, Lightning Node Software, Lightning Network Architecture, Onion Routing, Gossip and the Channel Graph) directly from the book's O'Reilly listing, and generalized only the unconfirmed gaps between them rather than inventing specific unconfirmed chapter numbers. For Virtual Society, confirmed the opening chapter's real content (Göbekli Tepe, ancient monuments as early 'virtual worlds') and the book's stated central thesis from publisher/review sources, and built the rest of the entry from Narula's well-documented public commentary on virtual worlds rather than guessing chapter-by-chapter detail not found in search results. Flagged this verification basis explicitly in both entries' `sourceNotes`, distinguishing it from entries written purely from existing general knowledge.
80. **Left Biographies — Religious/Spiritual at zero entries rather than force-fitting a book into it.** Checked the corrected 376-title catalog's "By Section" breakdown directly — there is no section for this category at all, meaning no catalog title was ever placed there; the closest-fit content (Thich Nhat Hanh's books) already has its own dedicated category since Session 6, by design (`DECISIONS.md` #66-74). This isn't a content gap Claude introduced or failed to close — it reflects that Thai doesn't currently own a book that fits this specific category. Flagged for Thai in `ROADMAP.md` in case he has a specific religious/spiritual-figure biography in mind (or picks one up later) that should be added to the catalog and then written up.
81. **Continued straight into a fourth content batch in the same session without pausing to ask again**, per Thai's explicit "do whatever you think is best" — read this as authorization to keep applying the same judgment already exercised earlier in the session (batch selection favoring thin-category coverage, skip-and-flag for anything not confidently known, validate-build-commit-push per batch) rather than a one-time approval for the single batch already in flight.
82. **When Thai asked for links to review the deployed app's look, found the production URL (`book-library-app-abundance-citadel.vercel.app`) sits behind Vercel's own account-level authentication (Vercel Authentication / SSO protection), not a password Thai set himself.** Attempted the MCP's shareable-link bypass tool, but it still redirected to Vercel's SSO handshake when fetched programmatically — this is expected, since that flow needs a real browser to complete the redirect-and-cookie exchange, not a raw HTTP fetch; it should work fine when Thai (already the Vercel team owner) opens the link himself. Did not disable deployment protection unilaterally to make the link work for a plain fetch, since that's a real access-control setting change — flagged it as an option in the Session 8 continuation prompt instead, since the GitHub repo and its content are already public, so removing Vercel's auth wall would add little additional exposure, but it's still Thai's call to make.
83. **Thai reported the link genuinely wasn't working, and confirmed via dashboard screenshots that he'd turned off both Vercel Authentication and Password Protection** — deployment protection was not actually the cause of the outage. Kept investigating rather than assuming the fix had "worked" once protection was off: reviewed the build logs (clean, all 87 pages including `/` generated successfully), forced a fresh no-cache deployment via a trivial commit (still 404'd identically, ruling out stale build cache), then asked Thai for Build & Deployment settings screenshots. Root cause found: the Vercel project's **Framework Preset was set to "Other" instead of "Next.js,"** which made Vercel serve the project as a generic static site (looking for a `public`/root directory of static files) while completely ignoring the actual Next.js server/serverless build output — explaining why the build logs showed success but literally nothing served. Thai corrected the dropdown to "Next.js" and saved. No MCP tool existed to read or change this setting directly, so this required Thai's own dashboard access throughout — logged the full diagnostic trail here so a future session doesn't have to rediscover it if the setting ever regresses (e.g., after a project re-import).
84. **Confirmed with Thai that Vercel Authentication should stay off rather than being re-enabled** now that the real bug (Framework Preset) is fixed — the two issues were unrelated, and since the GitHub repo/content are already public, leaving the Vercel URL open adds negligible exposure while removing login friction during this active design-review phase. Worth revisiting only at Stage 13 (Launch) if Thai wants the live URL itself gated separately from the already-public source.
85. **When Thai gave extensive, specific verbal feedback on design and content depth (color scheme, typography, collapsible sections, tabbed book detail, per-chapter key lessons, expanded quotes, author bios) and asked to restructure it into a continuation prompt for a fresh chat rather than implement it in this already-long session, did exactly that rather than starting the redesign work itself.** Rewrote `docs/SESSION_8_CONTINUATION_PROMPT.md` in full (superseding the earlier "check for feedback" placeholder version from decision #82's session) with his feedback organized into concrete sections: visual design, home page structure, book detail tabs, content-depth/schema changes, and a suggested execution order — plus added a new `ROADMAP.md` Stage 15 to track it as real project scope rather than letting the feedback live only in chat history. Deliberately did not start implementing any of it (no design research, no schema edits, no component rewrites) since Thai explicitly asked for the prompt to be written and handed to a new chat, not executed here.

---

### 2026-07-24 — Session 8 (design system v2, schema v2, UI rebuild, Atomic Habits migration)

86. **Chose gold (primary) + slate-teal (secondary) rather than picking one of Thai's three example combinations verbatim (orange+blue, blue+orange, gold-on-dark), since he explicitly invited a better-reasoned alternative if one existed.** A fully saturated orange+blue on near-black read, in a quick mockup pass, as sporty/corporate rather than calm/literary — desaturating orange toward gold keeps the warmth (candlelight association, fits a reading app) while sitting calmly on the dark background instead of vibrating against it, and desaturating blue toward slate-teal gives a genuine but muted cool counterpoint that won't fight gold for visual priority. Full rationale and exact hex scales in `docs/DESIGN_SYSTEM.md`.
87. **Implemented dark-first via CSS custom properties flipped by an opt-in `.light` class, not by keeping Tailwind's `dark:` prefix convention and inverting which state is "unclassed."** This was the more mechanical way to guarantee no component could silently regress back to a light-default look through a stray missing `dark:` prefix — with the variable approach, "dark" is simply what every base utility class resolves to unless `.light` is present, so there is no such thing as a component that forgot to opt into dark mode. Cost: a few two-tone chip elements (badges) needed their own dedicated CSS variables (`--badge-gold-bg/-fg` etc.) since a single flipped variable isn't enough when a chip needs different absolute colors (not just inverted lightness) per theme — documented in `docs/DESIGN_SYSTEM.md` and `app/components/Badge.tsx`.
88. **Chose `next/font/google` (Inter + Newsreader) over continuing the Stage 3 system-font-stack-only policy, treating "zero extra network dependency" as meaning zero *runtime* dependency, not zero *build-time* download.** `next/font/google` fetches and self-hosts font files once at build time; the deployed app makes no runtime request to any font CDN, which is the actual concern the Stage 3 rule was protecting against (page-load latency, privacy/tracking exposure to Google's font CDN on every visit). This is different in kind from a `<link>`-tag Google Fonts embed, which is what decisions #8–9's "no Google Fonts" language was really about. Documented the distinction explicitly in `docs/DESIGN_SYSTEM.md` v2 so a future session doesn't misread this as contradicting the old rule.
89. **Could not verify the `next/font/google` build step inside this sandbox** — `curl` confirmed the sandbox's network proxy returns a 403 for both `fonts.googleapis.com` and `fonts.gstatic.com` specifically (not a general outage; `github.com` was reachable in the same test), so `npm run build` fails here with a font-fetch error. Rather than abandoning the font choice or guessing whether it'll work in production, ran a throwaway local build with the two font-loader calls temporarily stubbed to dummy objects **only in a `/tmp` scratch copy** (never in the real synced source, which keeps the real `next/font/google` calls), confirmed all 87 pages/components/JSON validate and render correctly, and am relying on Vercel's own build log (unrestricted egress, and `next/font/google` is one of the most common patterns deployed there) for the actual font-fetch confirmation once this is pushed. Flagging this clearly rather than silently asserting the build is "fully verified" when one specific piece genuinely isn't, from this sandbox.
90. **Kept `/category/[slug]` pages rather than removing them when the home page's category grid became an accordion**, even though the brief left this open ("use judgment... flag the tradeoff"). The accordion is the fast browse surface; the category page still does two things the accordion doesn't: shows the "on the shelf, not yet summarized" list (the 376-title catalog feature from Session 6) and gives every category a stable, shareable URL. Each accordion section links out to its category page rather than duplicating the unwritten-list there, so there's no redundant UI, just two surfaces with slightly different jobs.
91. **Built `CategoryAccordion` on native `<details>`/`<summary>` instead of a `useState`-based accordion**, so the home page's primary browse interaction needs zero client-side JavaScript and is keyboard/accessibility-correct without extra ARIA wiring — consistent with the project's general minimal-dependency bias (decisions #8, #9, #14, #24). `BookTabs`, by contrast, does need to be a client component (tab content is genuinely conditional, and hash-syncing needs `window`), so that's the one new interactive piece introduced this session.
92. **Set `Quote.category` and `Section.keyLessons` as optional fields on the `Book` TypeScript type**, rather than required, specifically so the 66 pre-v2 book entries keep compiling and rendering with zero changes needed right now — the Author tab shows a plain "not written yet" state and the Quotes tab falls back to a single "General" bucket when these fields are absent. This was a deliberate choice to decouple "ship the v2 UI/schema" from "decide the backfill scope," since the backfill question (below) hadn't been answered yet when this code was written.
93. **For the Atomic Habits quote expansion (3 → 28), fetched James Clear's own published quote archive (jamesclear.com/quote/atomic-habits) and Goodreads' work-quotes page directly, rather than relying on memorized quotes, given the copyright policy is explicit that quotes are the one field allowed verbatim and the volume increase (20–30 per book) raises the stakes of getting a quote wrong.** Used only the quotes on James Clear's own site that were explicitly tagged as sourced from the Atomic Habits book (excluded a newsletter-only quote from the same page that wasn't tagged to the book). Ended at 28 rather than forcing exactly 30, consistent with `docs/CONTENT_PIPELINE.md`'s "a strong 18 beats a padded 30" guidance — did not invent additional plausible-sounding lines to hit a round number.
94. **Researched James Clear's `authorBio` via targeted web search (Denison University's own alumni profile plus general biographical sources) rather than from memory**, since exact biographical facts (birth date, injury circumstances, university, honors) are exactly the kind of detail that's easy to misremember confidently and wrong. Left `notableWorks` as an empty array rather than inventing or padding a bibliography — Clear is genuinely known for one book plus his newsletter, and the schema explicitly allows an empty array for that case.
95. **Asked Thai directly whether to retrofit the other 65 books to v2 depth, rather than picking a default and proceeding**, exactly as the Session 8 brief instructed — this is a large, real time-cost decision (`docs/CONTENT_PIPELINE.md` documents the v2 depth as a multi-x effort increase per book) and the brief was explicit that it should be a concrete question informed by seeing one migrated example (Atomic Habits), not a judgment call made on Thai's behalf.
